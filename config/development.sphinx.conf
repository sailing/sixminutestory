indexer
{
}

searchd
{
  address = 127.0.0.1
  port = 3312
  log = /Volumes/STUFF/Sites/6ms/log/searchd.log
  query_log = /Volumes/STUFF/Sites/6ms/log/searchd.query.log
  pid_file = /Volumes/STUFF/Sites/6ms/log/searchd.development.pid
}

source story_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = 
  sql_db = sixminutestory_development
  sql_sock = /tmp/mysql.sock
  sql_query_pre = UPDATE `stories` SET `delta` = 0
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `stories`.`id` * 1 + 0 AS `id` , CAST(`stories`.`title` AS CHAR) AS `title`, CAST(`stories`.`description` AS CHAR) AS `description`, CAST(`stories`.`license` AS CHAR) AS `license`, CAST(GROUP_CONCAT(DISTINCT `tags`.`name` SEPARATOR ' ') AS CHAR) AS `tag`, CAST(GROUP_CONCAT(DISTINCT `comments`.`comment` SEPARATOR ' ') AS CHAR) AS `comments`, IFNULL(`stories`.`title`, '') AS `title_sort`, IFNULL(`stories`.`license`, '') AS `license_sort`, CRC32(`stories`.`license`) AS `license_facet`, GROUP_CONCAT(DISTINCT `tags`.`id` SEPARATOR ',') AS `tag_ids`, UNIX_TIMESTAMP(`stories`.`updated_at`) AS `updated_at`, UNIX_TIMESTAMP(`stories`.`created_at`) AS `created_at`, `stories`.`active` AS `active`, `stories`.`id` AS `sphinx_internal_id`, 714550076 AS `class_crc`, '714550076' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM stories    LEFT OUTER JOIN `taggings` ON (`stories`.`id` = `taggings`.`taggable_id` AND `taggings`.`taggable_type` = 'Story')  LEFT OUTER JOIN `tags` ON (`tags`.`id` = `taggings`.`tag_id`)   LEFT OUTER JOIN `comments` ON comments.story_id = stories.id  WHERE `stories`.`id` >= $start   AND `stories`.`id` <= $end    AND `stories`.`delta` = 0 GROUP BY `stories`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `stories` WHERE `stories`.`delta` = 0
  sql_attr_uint = license_facet
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_bool = active
  sql_attr_timestamp = updated_at
  sql_attr_timestamp = created_at
  sql_attr_str2ordinal = title_sort
  sql_attr_str2ordinal = license_sort
  sql_attr_multi = uint tag_ids from field
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `stories` WHERE  `id` = (($id - 0) / 1)
}

index story_core
{
  source = story_core_0
  path = /Volumes/STUFF/Sites/6ms/db/sphinx/development/story_core
  morphology = stem_en
  charset_type = utf-8
}

source story_delta_0 : story_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = 
  sql_db = sixminutestory_development
  sql_sock = /tmp/mysql.sock
  sql_query_pre = 
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `stories`.`id` * 1 + 0 AS `id` , CAST(`stories`.`title` AS CHAR) AS `title`, CAST(`stories`.`description` AS CHAR) AS `description`, CAST(`stories`.`license` AS CHAR) AS `license`, CAST(GROUP_CONCAT(DISTINCT `tags`.`name` SEPARATOR ' ') AS CHAR) AS `tag`, CAST(GROUP_CONCAT(DISTINCT `comments`.`comment` SEPARATOR ' ') AS CHAR) AS `comments`, IFNULL(`stories`.`title`, '') AS `title_sort`, IFNULL(`stories`.`license`, '') AS `license_sort`, CRC32(`stories`.`license`) AS `license_facet`, GROUP_CONCAT(DISTINCT `tags`.`id` SEPARATOR ',') AS `tag_ids`, UNIX_TIMESTAMP(`stories`.`updated_at`) AS `updated_at`, UNIX_TIMESTAMP(`stories`.`created_at`) AS `created_at`, `stories`.`active` AS `active`, `stories`.`id` AS `sphinx_internal_id`, 714550076 AS `class_crc`, '714550076' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM stories    LEFT OUTER JOIN `taggings` ON (`stories`.`id` = `taggings`.`taggable_id` AND `taggings`.`taggable_type` = 'Story')  LEFT OUTER JOIN `tags` ON (`tags`.`id` = `taggings`.`tag_id`)   LEFT OUTER JOIN `comments` ON comments.story_id = stories.id  WHERE `stories`.`id` >= $start   AND `stories`.`id` <= $end    AND `stories`.`delta` = 1 GROUP BY `stories`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `stories` WHERE `stories`.`delta` = 1
  sql_attr_uint = license_facet
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_bool = active
  sql_attr_timestamp = updated_at
  sql_attr_timestamp = created_at
  sql_attr_str2ordinal = title_sort
  sql_attr_str2ordinal = license_sort
  sql_attr_multi = uint tag_ids from field
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `stories` WHERE  `id` = (($id - 0) / 1)
}

index story_delta : story_core
{
  source = story_delta_0
  path = /Volumes/STUFF/Sites/6ms/db/sphinx/development/story_delta
}

index story
{
  type = distributed
  local = story_delta
  local = story_core
}
