indexer
{
}

searchd
{
  address = 127.0.0.1
  port = 3312
  log = /Users/galen/Sites/sa/log/searchd.log
  query_log = /Users/galen/Sites/sa/log/searchd.query.log
  pid_file = /Users/galen/Sites/sa/log/searchd.development.pid
}

source share_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = 
  sql_db = sharearchy_development
  sql_sock = /tmp/mysql.sock
  sql_query_pre = UPDATE `shares` SET `delta` = 0
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `shares`.`id` * 1 + 0 AS `id` , CAST(`shares`.`title` AS CHAR) AS `title`, CAST(`shares`.`description` AS CHAR) AS `description`, CAST(`shares`.`license` AS CHAR) AS `license`, CAST(`shares`.`source` AS CHAR) AS `source`, CAST(GROUP_CONCAT(DISTINCT `tags`.`name` SEPARATOR ' ') AS CHAR) AS `tag`, CAST(GROUP_CONCAT(DISTINCT `comments`.`comment` SEPARATOR ' ') AS CHAR) AS `comments`, IFNULL(`shares`.`title`, '') AS `title_sort`, IFNULL(`shares`.`license`, '') AS `license_sort`, CRC32(`shares`.`license`) AS `license_facet`, IFNULL(`shares`.`source`, '') AS `source_sort`, CRC32(`shares`.`source`) AS `source_facet`, GROUP_CONCAT(DISTINCT `tags`.`id` SEPARATOR ',') AS `tag_ids`, UNIX_TIMESTAMP(`shares`.`updated_at`) AS `updated_at`, UNIX_TIMESTAMP(`shares`.`created_at`) AS `created_at`, `shares`.`active` AS `active`, `shares`.`id` AS `sphinx_internal_id`, 784839262 AS `class_crc`, '784839262' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM shares    LEFT OUTER JOIN `taggings` ON (`shares`.`id` = `taggings`.`taggable_id` AND `taggings`.`taggable_type` = 'Share')  LEFT OUTER JOIN `tags` ON (`tags`.`id` = `taggings`.`tag_id`)   LEFT OUTER JOIN `comments` ON comments.share_id = shares.id  WHERE `shares`.`id` >= $start   AND `shares`.`id` <= $end    AND `shares`.`delta` = 0 GROUP BY `shares`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `shares` WHERE `shares`.`delta` = 0
  sql_attr_uint = license_facet
  sql_attr_uint = source_facet
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_bool = active
  sql_attr_timestamp = updated_at
  sql_attr_timestamp = created_at
  sql_attr_str2ordinal = title_sort
  sql_attr_str2ordinal = license_sort
  sql_attr_str2ordinal = source_sort
  sql_attr_multi = uint tag_ids from field
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `shares` WHERE  `id` = (($id - 0) / 1)
}

index share_core
{
  source = share_core_0
  path = /Users/galen/Sites/sa/db/sphinx/development/share_core
  morphology = stem_en
  charset_type = utf-8
}

source share_delta_0 : share_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = 
  sql_db = sharearchy_development
  sql_sock = /tmp/mysql.sock
  sql_query_pre = 
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `shares`.`id` * 1 + 0 AS `id` , CAST(`shares`.`title` AS CHAR) AS `title`, CAST(`shares`.`description` AS CHAR) AS `description`, CAST(`shares`.`license` AS CHAR) AS `license`, CAST(`shares`.`source` AS CHAR) AS `source`, CAST(GROUP_CONCAT(DISTINCT `tags`.`name` SEPARATOR ' ') AS CHAR) AS `tag`, CAST(GROUP_CONCAT(DISTINCT `comments`.`comment` SEPARATOR ' ') AS CHAR) AS `comments`, IFNULL(`shares`.`title`, '') AS `title_sort`, IFNULL(`shares`.`license`, '') AS `license_sort`, CRC32(`shares`.`license`) AS `license_facet`, IFNULL(`shares`.`source`, '') AS `source_sort`, CRC32(`shares`.`source`) AS `source_facet`, GROUP_CONCAT(DISTINCT `tags`.`id` SEPARATOR ',') AS `tag_ids`, UNIX_TIMESTAMP(`shares`.`updated_at`) AS `updated_at`, UNIX_TIMESTAMP(`shares`.`created_at`) AS `created_at`, `shares`.`active` AS `active`, `shares`.`id` AS `sphinx_internal_id`, 784839262 AS `class_crc`, '784839262' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM shares    LEFT OUTER JOIN `taggings` ON (`shares`.`id` = `taggings`.`taggable_id` AND `taggings`.`taggable_type` = 'Share')  LEFT OUTER JOIN `tags` ON (`tags`.`id` = `taggings`.`tag_id`)   LEFT OUTER JOIN `comments` ON comments.share_id = shares.id  WHERE `shares`.`id` >= $start   AND `shares`.`id` <= $end    AND `shares`.`delta` = 1 GROUP BY `shares`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `shares` WHERE `shares`.`delta` = 1
  sql_attr_uint = license_facet
  sql_attr_uint = source_facet
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_bool = active
  sql_attr_timestamp = updated_at
  sql_attr_timestamp = created_at
  sql_attr_str2ordinal = title_sort
  sql_attr_str2ordinal = license_sort
  sql_attr_str2ordinal = source_sort
  sql_attr_multi = uint tag_ids from field
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `shares` WHERE  `id` = (($id - 0) / 1)
}

index share_delta : share_core
{
  source = share_delta_0
  path = /Users/galen/Sites/sa/db/sphinx/development/share_delta
}

index share
{
  type = distributed
  local = share_delta
  local = share_core
}
