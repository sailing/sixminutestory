- if flash[:notice] || flash[:alert] || flash[:error] || flash[:update_password]
  %div.mt-3
    - if flash[:error] && flash[:error].present?
      %div.alert.alert-danger.col-8.offset-2.mb-0.shadow.border-danger.fade.show
        %a.close(data-dismiss="alert") ×
        %h3.alert-heading Hello, what's this?!
        - flash[:error].each do |name, msg|
          - Array(msg).uniq.each do |key, message|
            = "#{name.capitalize} #{key.to_s}"
      - flash[:error] = nil

    - if flash[:notice] && flash[:notice].present?
      %div.alert.alert-info.col-8.offset-2.mb-0.shadow.border-info.fade.show
        %a.close(data-dismiss="alert") ×
        %h3.alert-heading
          = flash[:notice]

      - flash[:notice] = nil

    - if flash[:alert] && flash[:alert].present?
      %div.alert.alert-warning.col-8.offset-2.mb-0.shadow.border-warning.fade.show
        %a.close(data-dismiss="alert") ×
        %h3.alert-heading
          Heads up:
          = flash[:alert]
      - flash[:alert] = nil