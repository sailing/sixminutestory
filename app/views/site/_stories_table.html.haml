.row-fluid
  .span12.expand
    %h5= stories_type + " Stories"
    %table.table.front-page-stories
      %tbody
        - stories.each do |s|
          %tr
            %td= link_to s.title, s
            %td
              - if stories_type == 'Active'
                = s.comments_count
                %i.fa.fa-comment
              - else
                = s.votes_for
                %i.fa.fa-star
    = link_to subset_stories_path(:subset => stories_type.downcase), class: "btn btn-inverse pull-right" do
      %span Read More
      %i.fa.fa-circle-arrow-right

