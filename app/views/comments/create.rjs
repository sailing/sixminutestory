page.insert_html :bottom, :comments, :partial => 'comment', :object => @comment if @comment
page.replace_html 'comments_count', link_to(pluralize((@comment.story.comments_count + 1), 'remark'),{:controller => 'stories', :action => 'show', :id => @comment.story, :anchor => 'comments'}) if @comment 
page.replace_html 'comments_in_footer', link_to(pluralize((@comment.story.comments_count + 1), 'remark'),{:controller => 'stories', :action => 'show', :id => @comment.story, :anchor => 'comments'}) if @comment
page.replace_html 'comments_in_header', link_to(pluralize((@comment.story.comments_count + 1), 'remark'),{:controller => 'stories', :action => 'show', :id => @comment.story, :anchor => 'comments'}) if @comment

page.visual_effect :highlight,'comment_'+@comment.id.to_s if @comment

page[:comments_form].reset if @comment
