	<tr>
				<td>
					<%= truncate(firstline.refcode, :length => 100) %>
				</td>
				<td><%= link_to (h(firstline.stories_count) + " stories"), prompt_url(firstline) %></td>
				<td>
					<%= link_to "click to use", write_to_prompt_url(firstline) %>
					| <%= link_to "details", prompt_url(firstline) -%>
					<% if is_admin %>

						| <%= link_to "edit", edit_prompt_url(firstline) %>
					<% end %>
				</td>
			</tr>