	<tr>
				<td>
					<%= truncate(prompt.refcode,100) %>
				</td>
				<td><%= link_to (h(prompt.stories_count) + " stories"), prompt_url(prompt) %></td>
				<td>
					<%= link_to "click to use", write_to_prompt_url(prompt) %>
					| <%= link_to "details", prompt_url(prompt) -%>
					<% if is_admin %>

						| <%= link_to "edit", edit_prompt_url(prompt) %>
					<% end %>
				</td>
			</tr>