<% @user == current_user ? name = "You have" : @user.login + " has"%>
<h1><%= name %> <%= @user.votes.count %> favorites</h1>

Sort by: 
			<div class="brown button_container"><a href="?order=counter%20DESC">Popular</a></div> 
			<div class="brown button_container"><a href="?order=votes.created_at%20DESC">Recent</a> </div> 
			<div class="brown button_container"><a href="?order=votes.created_at%20ASC">Earliest</a> </div>

<%= render :partial => "layouts/show_stories" %>

<%= render :partial => "users/profile" %>